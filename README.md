# TODO App Server (Vapor + Docker)

## Запуск

```bash
docker compose up --build
```

Открыть в браузере: http://localhost:8080/todos

## Методы

- GET /todos
- POST /todos
- PUT /todos/:id
- DELETE /todos/:id
- GET /config